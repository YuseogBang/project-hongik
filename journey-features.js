/* Remaining mockup screens: save gate, tag review, route and desktop workbench. */
(() => {
  const css = document.createElement('style');
  css.textContent = `.jf-card{margin:14px 0 0;padding:14px;border:1px solid #7a2534;border-radius:15px;background:#3d0f16;color:#f5ece7;font-family:Pretendard,sans-serif}.jf-card h4{margin:0 0 8px;font-size:17px}.jf-card p{margin:0;color:#c39298;font-size:12px;line-height:1.55}.jf-buttons{display:flex;gap:7px;margin-top:12px}.jf-buttons button,.jf-tag{border:1px solid #7a2534;border-radius:10px;background:#4f151e;color:#f5ece7;padding:9px 10px;font:600 12px Pretendard,sans-serif}.jf-buttons .primary{border-color:#e8362a;background:#e8362a}.jf-tags{display:flex;flex-wrap:wrap;gap:6px;margin-top:10px}.jf-tag.on{border-color:#e8362a;background:rgba(232,54,42,.18);color:#ff93a2}.jf-review-input{display:block;width:100%;min-height:72px;box-sizing:border-box;margin-top:12px;padding:10px;border:1px solid #7a2534;border-radius:11px;background:#2b070c;color:#f5ece7;font:12px/1.5 Pretendard,sans-serif;resize:vertical}.jf-review-input::placeholder{color:#8a5f66}.jf-review-cta{display:block;margin:12px 0 0 auto!important;border:0!important;background:#e8362a!important;color:#fff!important;box-shadow:0 8px 18px rgba(232,54,42,.28)}.jf-review-list{margin-top:16px;padding-top:13px;border-top:1px solid #7a2534}.jf-review-item{padding:10px 0;border-bottom:1px solid #4f151e;font-size:12px;line-height:1.6}.jf-review-meta{display:block;margin-top:4px;color:#c39298;font-size:10px}.jf-place-tabs{display:flex;gap:4px;overflow-x:auto;padding:4px;margin-bottom:8px;border-radius:999px;background:#fff;box-shadow:0 2px 10px rgba(20,60,50,.1)}.jf-place-tabs button{flex:1;white-space:nowrap;border:0;border-radius:999px;padding:8px 11px;background:transparent;color:#5c756d;font:700 11px Pretendard,sans-serif}.jf-place-tabs button.on{background:var(--accent);color:#fff}.jf-place-panel[hidden]{display:none}.jf-info-row{display:flex;gap:9px;padding:11px 0;border-bottom:1px solid var(--border);font-size:12px;line-height:1.55;color:var(--text)}.jf-info-label{width:46px;flex:none;color:var(--muted);font-size:10px;font-weight:800;letter-spacing:.04em}@media(min-width:701px){.jf-rail{position:fixed;z-index:650;left:16px;top:124px;width:64px;padding:8px 0;border:1px solid #7a2534;border-radius:16px;background:rgba(43,7,12,.95);box-shadow:0 14px 36px rgba(0,0,0,.4)}.jf-rail button{width:100%;padding:11px 3px;border:0;background:transparent;color:#c39298;font:600 10px Pretendard,sans-serif}.jf-rail button:hover{color:#ff8a7a}.jf-desktop-results{display:none;position:fixed;z-index:610;top:124px;bottom:20px;left:94px;width:300px;overflow:auto;padding:16px;background:rgba(43,7,12,.96);border:1px solid #7a2534;border-radius:17px;box-shadow:0 16px 42px rgba(0,0,0,.45);color:#f5ece7;font-family:Pretendard,sans-serif}.jf-desktop-results.open{display:block}.jf-desktop-results h3{margin:0 0 12px;font-size:16px}.jf-desktop-results button{width:100%;padding:11px 0;border:0;border-bottom:1px solid #4f151e;background:transparent;color:#f5ece7;text-align:left;font:600 12px Pretendard,sans-serif}.jf-desktop-results small{display:block;color:#c39298;margin-top:4px;font-weight:400}}`;
  document.head.append(css);
  const $=(s,r=document)=>r.querySelector(s); const reviewKey='hongdaeTagReviews';
  const getReviews=()=>JSON.parse(localStorage.getItem(reviewKey)||'[]');
  function showOverlay(html){ const o=$('.xp-overlay')||Object.assign(document.createElement('div'),{className:'xp-overlay'}); if(!o.parentNode){o.addEventListener('click',e=>{if(e.target===o)o.classList.remove('open')});document.body.append(o)} o.innerHTML=html;o.classList.add('open');return o; }
  function close(){ $('.xp-overlay')?.classList.remove('open'); }
  function requestSave(id){ const s=stores.find(x=>x.id===id); const o=showOverlay(`<div class="xp-sheet"><div class="xp-handle"></div><div class="xp-top"><span class="xp-kicker">SAVE THIS PLACE</span><button class="xp-close">✕</button></div><h2 class="xp-title">저장한 가게는<br>기기가 바뀌어도 남아야 하니까요</h2><p class="xp-copy"><b style="color:#f5ece7">${s?.name||'이 가게'}</b>를 내 컬렉션에 담아둘까요? 이메일 로그인으로 동기화하거나, 이 기기에만 저장할 수 있어요.</p><button class="xp-primary" data-email>이메일 로그인으로 저장</button><button class="xp-secondary" data-local>이 기기에만 저장할게요</button><button class="xp-secondary" data-later>나중에</button></div>`);$('.xp-close',o).onclick=close;$('[data-later]',o).onclick=close;$('[data-email]',o).onclick=()=>{localStorage.setItem('pendingSaveId',id);close();HongdaePlatform.openDialog()};$('[data-local]',o).onclick=()=>{localStorage.setItem('localSaveConsent','1');close();toggleBookmark(id)}; }
  const reviewTags=['가성비','분위기','응대','웨이팅','혼밥','데이트','조용함','콘센트','재방문','사진맛집'];
  const scoreLabel={again:'또 간다',okay:'보통',no:'아니다'};
  const escape=(v)=>String(v||'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  function reviewList(id){const items=getReviews().filter(r=>r.id===id);return items.length?`<div class="jf-review-list"><b style="font-size:12px">이 장소의 리뷰 ${items.length}건</b>${items.map(r=>`<div class="jf-review-item"><b>${scoreLabel[r.score]||'방문 기록'}</b>${r.tags?.length?` · ${r.tags.map(escape).join(' · ')}`:''}${r.note?`<div>${escape(r.note)}</div>`:''}<span class="jf-review-meta">${new Date(r.at).toLocaleDateString('ko-KR')}</span></div>`).join('')}</div>`:'<div class="jf-review-list"><span class="jf-review-meta">첫 리뷰를 남겨주세요.</span></div>'}
  function injectReview(){ const id=selectedId; const host=$('#detail-content'); const panel=host?.querySelector('[data-review-panel]'); if(cardStyle!=='A'||!id||!host||!panel)return; panel.querySelector('.jf-card')?.remove(); const box=document.createElement('section');box.className='jf-card';box.innerHTML=`<h4>다녀오셨나요?</h4><p>선택과 태그, 한 줄 후기로 내 취향 추천에 반영돼요.</p><div class="jf-buttons"><button data-score="again" class="primary">또 간다</button><button data-score="okay">보통</button><button data-score="no">아니다</button></div><div class="jf-tags">${reviewTags.map(t=>`<button class="jf-tag">${t}</button>`).join('')}</div><textarea class="jf-review-input" maxlength="500" placeholder="직접 남기는 한 줄 리뷰 (선택)"></textarea><button data-submit class="jf-review-cta">기록 남기기 →</button>${reviewList(id)}`;let score=null;const tags=new Set();box.querySelectorAll('[data-score]').forEach(b=>b.onclick=()=>{score=b.dataset.score;box.querySelectorAll('[data-score]').forEach(x=>x.classList.remove('primary'));b.classList.add('primary')});box.querySelectorAll('.jf-tag').forEach(b=>b.onclick=()=>{b.classList.toggle('on');b.classList.contains('on')?tags.add(b.textContent):tags.delete(b.textContent)});$('[data-submit]',box).onclick=()=>{if(!score){showToast('먼저 한 가지를 골라주세요');return}const note=box.querySelector('.jf-review-input').value.trim();const all=getReviews();all.unshift({id,score,tags:[...tags],note,at:Date.now()});localStorage.setItem(reviewKey,JSON.stringify(all));if(score==='again'){tags.forEach(t=>{if(TASTE_TAGS.includes(t)&&!userTastes.includes(t))userTastes.push(t)});localStorage.setItem('userTastes',JSON.stringify(userTastes))}showToast('리뷰와 취향 기록을 저장했어요');injectReview()};panel.append(box); }
  function enhanceDetail(){const host=$('#detail-content');const s=stores.find(x=>x.id===selectedId);const body=host?.querySelector('.detail-body');if(!s||!body||cardStyle!=='A'||body.querySelector('.jf-place-tabs'))return;const tabs=document.createElement('nav');tabs.className='jf-place-tabs';tabs.innerHTML='<button class="on" data-tab="overview">개요</button><button data-tab="menu">메뉴</button><button data-tab="reviews">리뷰</button><button data-tab="info">정보</button>';const overview=document.createElement('section');overview.className='jf-place-panel';overview.dataset.placePanel='overview';const menu=document.createElement('section');menu.className='jf-place-panel';menu.dataset.placePanel='menu';const reviews=document.createElement('section');reviews.className='jf-place-panel';reviews.dataset.placePanel='reviews';reviews.dataset.reviewPanel='';reviews.hidden=true;const info=document.createElement('section');info.className='jf-place-panel';info.dataset.placePanel='info';info.hidden=true;const children=[...body.children];const maps=children.find(el=>el.querySelector('.map-btn'));const menuBlock=children.find(el=>el.hasAttribute('data-place-menu'));children.filter(el=>el!==maps&&el!==menuBlock).forEach(el=>overview.append(el));if(menuBlock)menu.append(menuBlock);else menu.innerHTML='<div class="jf-info-row">대표 메뉴 정보가 아직 없어요.</div>';info.innerHTML=`<div class="jf-info-row"><b class="jf-info-label">주소</b><span>${escape(s.address||s.dong||'주소 정보 없음')}</span></div><div class="jf-info-row"><b class="jf-info-label">영업시간</b><span>${escape(s.hours||'지도에서 확인 필요')}</span></div>${s.hoursNote?`<p class="jf-hours-note">${escape(s.hoursNote)} · 방문 전 지도에서 다시 확인해 주세요.</p>`:''}<div class="jf-info-row"><b class="jf-info-label">분류</b><span>${escape(s.category||'홍대 가게')}</span></div>`;if(maps)info.append(maps);body.prepend(tabs);body.append(overview,menu,reviews,info);tabs.querySelectorAll('button').forEach(button=>button.onclick=()=>{const tab=button.dataset.tab;tabs.querySelectorAll('button').forEach(b=>b.classList.toggle('on',b===button));body.querySelectorAll('[data-place-panel]').forEach(p=>p.hidden=p.dataset.placePanel!==tab);if(tab==='reviews')injectReview()}); }
  function openRoute(){ const candidates=Object.keys(bookmarks).map(Number).map(id=>stores.find(s=>s.id===id)).filter(Boolean); const route=(candidates.length?candidates:stores.filter(s=>userTastes.some(t=>(s.tags||[]).includes(t))).slice(0,4)).slice(0,4); const o=showOverlay(`<div class="xp-sheet"><div class="xp-handle"></div><div class="xp-top"><span class="xp-kicker">MY TASTE ROUTE</span><button class="xp-close">✕</button></div><h2 class="xp-title">취향 루트</h2><p class="xp-copy">${candidates.length?'저장한 가게':'내 취향에 맞는 가게'}를 동선으로 묶었어요 · ${route.length}곳</p><div style="margin-top:14px">${route.map((s,i)=>`<button class="jf-route-step" data-route="${s.id}"><span class="jf-route-num">${i+1}</span><span><b>${s.name}</b><small style="display:block;color:#c39298;margin-top:4px">${s.category||TYPES[s.type]||'홍대 가게'} · ${s.dong||'홍대'}</small></span></button>`).join('')||'<p class="xp-copy">저장한 가게나 취향 태그를 먼저 만들어주세요.</p>'}</div><button class="xp-primary" data-start ${route.length?'':'disabled'}>첫 장소 지도에서 보기</button></div>`);$('.xp-close',o).onclick=close;o.querySelectorAll('[data-route]').forEach(b=>b.onclick=()=>{close();selectStore(Number(b.dataset.route))});$('[data-start]',o).onclick=()=>{if(route[0]){close();selectStore(route[0].id)}}; }
  function desktop(){if(innerWidth<701||$('.jf-rail'))return;const rail=document.createElement('nav');rail.className='jf-rail';rail.innerHTML='<button data-map>지도</button><button data-feed>피드</button><button data-save>저장</button><button data-route>루트</button><button data-me>나</button>';document.body.append(rail);const pane=document.createElement('aside');pane.className='jf-desktop-results';document.body.append(pane);const draw=()=>{const a=getFilteredStores().slice(0,20);pane.innerHTML=`<h3>결과 ${a.length}곳</h3>${a.map(s=>`<button data-id="${s.id}">${s.name}<small>${s.category||TYPES[s.type]||''} · ${(s.tags||[]).slice(0,2).join(' · ')}</small></button>`).join('')}`;pane.querySelectorAll('[data-id]').forEach(b=>b.onclick=()=>selectStore(Number(b.dataset.id)));pane.classList.add('open')};$('[data-map]',rail).onclick=()=>pane.classList.remove('open');$('[data-feed]',rail).onclick=openCuratedFeed;$('[data-save]',rail).onclick=()=>{showBookmarks();draw()};$('[data-route]',rail).onclick=openRoute;$('[data-me]',rail).onclick=()=>HongdaeExperience.openProfile();document.addEventListener('click',e=>{if(e.target.closest('.category-bar .chip')||e.target.closest('#chip-filter'))setTimeout(draw,0)});}
  // 상세 화면은 탭으로 쪼개지지 않고 개요 흐름을 그대로 유지한다.
  enhanceDetail=()=>{};
  const base=HongdaeExperience.recordView;HongdaeExperience.recordView=(id)=>{base(id);setTimeout(()=>{enhanceDetail();injectReview()},0)};Object.assign(HongdaeExperience,{requestSave,openRoute});document.addEventListener('DOMContentLoaded',desktop);
})();
